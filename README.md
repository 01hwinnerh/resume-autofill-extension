# Resume Autofill Assistant

A local-first, review-first browser extension shell for job-application forms.

## Commands

```bash
pnpm dev
pnpm build
pnpm typecheck
pnpm test
pnpm e2e
```

The shell does not scan, fill, submit, upload, or send data to a backend.
